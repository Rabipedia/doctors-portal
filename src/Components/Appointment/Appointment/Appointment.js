import React from 'react';
import Navbar from '../../Shared/Navbar/Navbar';

const Appointment = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};

export default Appointment;